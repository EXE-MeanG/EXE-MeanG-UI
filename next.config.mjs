/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "d1hjkbq40fs2x4.cloudfront.net",
      "res.cloudinary.com",
      "s3.amazonaws.com",
    ],
  },
};

export default nextConfig;
